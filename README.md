# AI Travel Planner

Monorepo with a Next.js (TypeScript + Tailwind) frontend and an Express (TypeScript) backend.

Run frontend:
```bash
cd frontend
npm install
npm run dev
```

Run backend:
```bash
cd backend
npm install
npm run dev
```
